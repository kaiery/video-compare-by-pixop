# P07 用户手动日志确认

来源：用户在对话中提供的日志。引擎 `20260828-reykjavik`，PID 70072；2026-09-19 09:03:06.476–09:07:33.814 UTC。以下为原日志的相关摘录，不是助手重新执行测试的结果。

```text
Left: [74,44], RGB(255,239,0#ffef00) ... Right: [89,59], RGB(0,19,255#0013ff) ...
Metrics: [00:01:02.500|00:01:02.500] PSNR(7.893), SSIM(0.26922), VMAF(1.374113)
Display state: window=480x320 aspect=stretch visual_left=160x96 visual_right=192x128 ref_dar=5:3
Saved window size (480x320)
Restored saved window size (480x320)
Restored startup window size (480x320)
Display state: window=480x320 aspect=stretch visual_left=12x10 visual_right=4x4 ref_dar=6:5
正常退出；退出码=0
```

像素行压缩了空格并以省略号省去 YUV 值，其余列出的事件保留原值。输入为本项目 P07 的两份无损测试素材；命令包含 `--auto-loop-mode on --frame-buffer-size 1`，这是固定帧测试会话，不是日常播放的推荐配置。

| ID | 可以确认 | 仍不能据此确认 | 记录结论 |
|---|---|---|---|
| RUN-008 | GUI 启动后像素、PSNR/SSIM/VMAF、Shift+X 显示状态均能进入日志 | X 的 FPS 显示是否可见；整组完整可视验收 | 关闭旧的“GUI 未收到 Display state”失败点；整组保留 BLOCKED，日志子项通过 |
| RUN-013 | 保存、恢复保存尺寸、恢复启动尺寸三个命令均被引擎处理 | 用户未明确确认拖动改变尺寸后窗口的实际恢复效果；没有变化前后的尺寸证据 | 日志子项通过；整组保留 BLOCKED，不冒称完整验收 |
| RUN-011 | 某次操作后左右显示尺寸变为 12×10、4×4，发生了可见内容尺寸变化 | 完整裁剪/复制/清除/撤销流程及等价 GUI/CLI 对照 | 不关闭原裁剪对照失败记录，也不据此认定引擎存在裁剪缺陷 |
| RUN-015 | 最后正常退出，退出码 0 | 无新增未确认条件 | 对既有 PASS 提供额外用户证据 |

用户明确表示没有完成全部操作，并授权助手决定后续。处理决定：停止向用户追加复杂手动清单，本轮 P07 以受限验收收尾；未经验证的部分继续保留在结果表，不改为 PASS。后续若开展专项回归，可使用现有清单，但不作为本轮要求用户继续操作的任务。

## 持续改进建议

- 日常使用选择“连续播放（不循环）”；保留本次测试会话及日志供专项回归，不将单帧循环误认为视频播放长度受限。
