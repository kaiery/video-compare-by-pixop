# P07 完整联调记录

日期：2026-09-19。**当前为部分验收，不能标记 P07 全部已验收。** 已完成参数、启动路径及实机输出对照；物理显示条件和完整交互验收的缺口逐项列在 [结果登记表](P07-RESULTS.md)。P08 已补齐 GUI-011 独立发布验证，最新总表为 97 PASS、13 BLOCKED、1 FAIL；下文 N/A 及较早统计是当时的历史记录，不代表发布仍未执行。

## 当前收尾决定（用户补充日志后）

用户提供 PID 70072 的手动运行日志，确认像素/指标/Display state、保存/恢复窗口尺寸命令和正常退出；见 [用户日志确认](P07-USER-VERIFICATION.md)。RUN-008 原先缺少 Display state 的失败点已被真实手动日志排除，整组因 FPS 可视效果未确认仍为 BLOCKED；RUN-013 日志子项通过，不冒称已经确认实际窗口恢复效果。

当前总表为 **96 PASS、13 BLOCKED、1 FAIL、1 N/A**。剩余 FAIL 是历史裁剪对照：操作状态不等价，不能直接归因为启动器缺陷，也不能用本次不完整操作记录关闭。用户不熟悉复杂测试并授权助手决定后续，因此**本轮以受限验收收尾，不再要求用户继续补测**；保留所有未验证项，既有手动清单仅供未来专项回归参考。以下安全续验及中断部分为历史记录，其统计以本节和最新结果表为准。

## 安全续验结果

本轮继续执行后，111 项登记为 **96 PASS、12 BLOCKED、2 FAIL、1 N/A**。BLOCKED 分别是手动交互或显示环境条件；两项 FAIL 保留历史失败，并未用最新的“无法自动执行”覆盖为通过。P07 尚待手动验收反馈，详见 [P07-MANUAL.md](P07-MANUAL.md)。本轮没有全局键鼠输入，也没有修改原项目代码。

| 工作 | 结果与证据 |
|---|---|
| 15 个运行组重新调度 | `work/p07/resume-safe/results.tsv` 完整记录 15 项：6 PASS、9 BLOCKED；正常完成，不再在 RUN-013 中断 |
| RUN-001 帮助/信息/隐藏左右/HUD | 横向布局下保存操作前、切换后、恢复后的图片；各状态确实变化且复原，14 个稳定画面跨 GUI/CLI 字节一致。HUD 的实测帧率文本不跨进程比较，但仍验证显示/隐藏和恢复。`resume-visual-hstack/RUN-001-keys` PASS |
| RUN-006 差异视图 | 原视频、差异、4 种子模式、亮度/彩色共 8 个画面状态跨 GUI/CLI 一致，差异和亮度切换的画面变化断言通过；`resume-visual-hstack/RUN-006-keys` PASS |
| RUN-015 正常退出 | 两条路径 `escape-exited=true`、退出码 0、截图一致；`resume-safe/RUN-015-keys` PASS。修正测试工具在窗口已响应 Escape 销毁后仍投递 key-up 的误报，不改变引擎 |
| RUN-008 失败分析 | 两条实际启动命令相同，PSNR/SSIM/VMAF 和左右帧相同；旧 GUI 路径缺少 Display state，而 CLI 路径有输出。定向组合键也不能可靠触发 SDL，故转为手动验证，不判定为已修复 |
| RUN-011 失败分析 | 两条实际命令相同，交互后的裁剪尺寸/状态不同；GUI 4 条、CLI 3 条 Display state，右侧出现 52×40 对 60×46。旧测试没有维持等价交互条件，仍需在相同裁剪区域手动复核 |
| 失败证据提取 | 可重跑 `tools/audit-p07-failures.ps1`；`work/p07/failure-audit.json` 包含实际命令比较、状态日志、图片尺寸及哈希比较 |
| 自动输入防回归 | `tools/verify-p07-input-safety.ps1` 已加入 P07 入口；检查禁止全局输入、前台焦点切换、输入线程附加、系统鼠标移动和广播；单键投递校验目标 PID |
| 构建 | 当前测试程序 Debug/Release 编译成功，日志 `work/p07/resume-final-build.log`；未改参数/GUI 实现，原 11 组核心测试通过证据保留 |

`resume-visual/` 中的首次中间截图检查发现：分割条会让一侧不可见，HUD 还包含实测帧率。这两类问题在固定横向布局和区分动态 HUD 后得到复验；保留旧记录，不将它们描述为 GUI 功能修复。全局键鼠注入始终保持禁用。

## 中断复核（用户报告 VS Code 闪退后）

- 最后一轮计划 10 项，实际只有 7 条完成记录（5 PASS、2 FAIL），最后时间 16:30:59；RUN-013 已创建目录但无完成结果，RUN-014/015 未进入该轮。没有残留联调程序、引擎或运行助手进程。P07 状态已明确改为 **未完成**。
- 最新 RUN-008 缺少所需显示状态/指标证据；RUN-011 的 GUI/CLI 输出帧不一致。此前通过记录不能覆盖最新失败，仍需区分输入时序/焦点与引擎行为。
- VS Code 原会话日志在 16:31:01.224 记录扩展宿主退出码 0，16:31:24/39 有重新启动记录。RUN-013 使用的 Ctrl+Shift+W 可能因焦点变化误发至编辑器，引发关闭窗口；这是时间线支持的推断，尚非已证实的崩溃根因。应用事件日志未查到这一时刻 Code.exe 崩溃记录；更早 15:13/14 的 GUI 错误记录不与本次混同。
- 已从联调测试源码移除全局键鼠输入与前台焦点操作；需要这类输入的用例在启动引擎前记为 BLOCKED，留给手动验收。检查本轮只重建测试程序，没有重新运行交互测试。
- 先前尚未生成的结果总表/JSON/CSV 在此次恢复检查中补齐；既有原始日志保留。末轮缺失的三项明确为 TODO，不冒用上一轮完成状态。

## 范围与证据含义

- 原项目代码、构建文件与依赖没有修改。本轮新增原生联调程序 `tests/engine_acceptance.cpp`、素材生成及复验脚本、结果与验收文档；全部在 `gui-launcher/`。
- 原生联调程序编译生产 `src/main.cpp`，通过实际控件恢复会话、检查 GUI 生成的 argv，再发送“开始比较”命令，经生产运行助手启动引擎。CLI 对照直接调用 `CreateProcessW`。这是生产 GUI 启动路径集成测试，不冒充对已安装 GUI EXE 的全部手工操作；安装版的文件选择、设置、会话与日志界面证据引用 P03–P06。
- 每组使用同一引擎、媒体、工作目录和参数。保存 `.vcgui`、实际命令、原始 stdout/stderr、窗口大小/数量、引擎截图与退出码。正常路径要求两侧截图中的左右帧 PNG 字节相等；不比较包含计时和通知的 OSD 截图。这个判定验证输出一致性，不代表每一个模式已经有独立的画质正确性判据。
- 第一轮 `work/p07/matrix/results.tsv` 为 132 组，原始记录保留。缺失字体在短暂创建窗口后失败，先前“绝不能创建窗口”的测试判定过严；截图按键增加有限重试；缓冲 50/150 改用无损固定画面，排除截图时刻和有损编码帧差异。复验位于 `work/p07/retest/`；合并表优先引用同 ID/变体的复验，不抹去历史。
- 组合键的投递消息测试未更新 SDL 使用的 Windows 键盘状态；真实输入第一次又遇到焦点限制。随后使用输入线程附加和扫描码输入重试。`interaction/`、`focus-retest/`、`scan-retest/` 保留诊断过程，最终复验在 `final-interaction/`。测试失败不直接归因为启动器缺陷；必须核对实际日志与操作是否到达引擎。
- `PASS` 仅对“实际观察”栏所列范围有效。运行快捷键冒烟只能证明操作序列后仍可运行和输出，不能证明中间状态、所有修饰键、剪贴板、鼠标选区正确，因此未完整验证的 RUN 行保留 TODO。不得用子用例 PASS 宣称整个 P07 完成。

## 环境与已核实条件

| 项目 | 实际情况 |
|---|---|
| 引擎 | 用户提供的 `D:\Tools\Media\video-compare-20260828-win10-x86_64\video-compare.exe`，版本 `20260828-reykjavik`，保留原配 DLL |
| 引擎库 | 日志：FFmpeg 9.0.1、SDL 2.32.10、SDL_ttf 2.24.0；与素材生成工具版本分别记录 |
| GPU | NVIDIA RTX 4090；CLI-055/056/057 及 RV-008 的原始日志出现 `h264 (cuda)`，并生成匹配帧 |
| AV1 | `libdav1d:export_side_data=film_grain` 解码合成 AV1 输入成功；不据此声称已经验证有胶片颗粒素材的具体效果 |
| VMAF | `vmaf_4k_v0.6.1` 模型实际计算，日志有 PSNR、SSIM、VMAF 数值 |
| 分析窗口 | 直方图、矢量示波器、波形窗口及其参数已执行 GUI/CLI 对照；观察文件记录 SDL 窗口数量 |
| 10-bpc | 引擎报告视频纹理 `ARGB2101010`，窗口仍为 `RGB888`；**不能确认物理 10 位输出** |
| 第二屏 | 系统枚举与 SDL 可用屏幕不同；引擎报 `displayIndex must be in the range 0 - 0`。显示编号 1 为 BLOCKED，编号 0 可用 |
| HDR | 合成 10-bit/PQ 标签素材用于色调映射路径；不是校准 HDR 源，不证明真实峰值亮度或显示器 HDR 输出 |
| DPI | 既有真实窗口证据为 96 DPI；未修改系统缩放，实际 150%/200% 仍待验证 |
| 原始用户视频 | 继承 P05/P06 的用户视频实机证据；本轮批量对照使用自生成固定画面，二者不混称 |

## 当前检查与可复现入口

- 本轮 `build.ps1 -Configuration All -Test -Install` 成功，Debug/Release 各 11 组 CTest 通过，完整日志 `work/p07/build-test.log`。
- `verify-coverage.ps1`、`verify-settings-coverage.ps1` 通过：61 CLI 选项及全部别名、11 RV 键、控件映射一致。
- [P07-RESULTS.md](P07-RESULTS.md) 按 111 个覆盖 ID 登记；原始对应关系仍在 [COVERAGE.md](COVERAGE.md) 与 [P04-CONTROLS.md](P04-CONTROLS.md)。JSON 位于 `work/p07/coverage-results.json`，子用例合并结果位于 `work/p07/merged-subcases.csv`。
- 本轮 GUI/运行助手哈希：`work/p07/binary-hashes.json`。素材工具版本及无损素材 SHA256：`work/p07/assets-lossless/`。

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\gui-launcher\tools\verify-p07.ps1 -Engine 'D:\Tools\Media\video-compare-20260828-win10-x86_64\video-compare.exe' -FFmpeg 'D:\Tools\DevTools\FFMPEG\ffmpeg-master-latest-win64-gpl\bin\ffmpeg.exe'
```

可用 `-CaseIds 'CLI-021,IN-001'` 限定复验；结果写入新 GUID 子目录。`-SkipBuild` 只适用于已确认当前二进制的情况。脚本失败即停止，不把之前的 EXE 检查当成本次构建通过；成功也不自动批准未验证的交互/物理显示项。

生成登记表时按时间顺序指定证据目录，后者覆盖相同 ID/变体：

```powershell
& .\gui-launcher\tools\write-p07-results.ps1
```

## 未完成项目的关闭条件

| 项目 | 补验方法及必须保留的证据 |
|---|---|
| RUN-002–004 | 在真实比较窗口逐项观察播放/循环/速度、寻址逐帧、放大镜/平移；时间剪贴板需要实际复制与粘贴往返。保存操作前后画面和时间信息；RUN-001 已由本轮中间画面验证补齐 |
| RUN-005 | Tab 已有完整 1+3/1+11 轮换日志；补验 Ctrl+Shift+1..0 直达目标、交换后独立滤镜及第 11 项回环 |
| RUN-007–010 | 对时间微调、像素/指标、选区导出、布局/比例/全屏，核对可见状态与直接 CLI；选区必须检查导出尺寸和像素。RUN-006 已由本轮 8 个画面状态补验 |
| RUN-011 | Shift+L/R/B 选择后裁剪；Ctrl+L/R 清除；Ctrl+Shift+L/R 复制；Backspace 撤销。逐步检查尺寸/过滤日志和输出帧，不能以“未崩溃”代替 |
| RUN-013 | 窗口尺寸保存/恢复日志已有自动化检查；还需核对实际保存尺寸和恢复尺寸，而非仅日志出现。RUN-012 已观察到分析窗口总数从 1→4→1，配置对照见 CLI-038–045 |
| RUN-014 | 分割条、左键定位、滚轮、右键拖动与 Ctrl/Shift 修饰分别对照位置、时间和画面 |
| CLI-010/014、GUI-008 | 在实际支持的 10 位显示链路、SDL 可见第二屏、Windows 150%/200% 缩放环境运行；记录真实设备/缩放及截图，禁止伪造 DPI 消息冒充实机 |
| GUI-011 | 独立构建与目录边界已核对；干净目录解压启动及依赖清单由 P08 补齐 |

## 持续改进建议

- 将上述交互步骤补成带前后状态断言的测试，再把对应 TODO 改为 PASS；保留本次合成素材和 GUI/CLI 输出作后续引擎升级回归基准。
